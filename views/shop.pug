doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{titleProb}
        link(rel="stylesheet", href="/nav.css")
    body 
      header.main-header 
        ul.nav-list 
          li.nav-list_li
            a(href="/").anker Shop 
          li.nav-list_li
            a(href="/admin/addProduct").anker Add Product
          li.nav-list_li 
            a(href='/ownProducts').anker Own Prodcuts

      main 
       each produkt in products
         p #{produkt.title}
         p #{produkt.price}$
         p #{produkt.description}
         p #{produkt.id}
         a(href=`/admin/edit-product/${produkt.id}`) Bearbeiten
         button(onclick=`deleteProduct(${produkt.id})`) Löschen
         br
         br

    script(src="/deleteScript.js")
